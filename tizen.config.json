{
  "version": "0.4.0",

  "comment": "Versioned paths under this origin are cached as immutable, so a version republished with different content fails its digest check on every TV.",
  "origin": "https://cdn.example.com/tube",
  "portsComment": "The proxy port is written in three places that must agree: here, service/lib/ports.js, and the --proxy switch in app/config.xml. npm run package fails if the last two drift.",
  "ports": {
    "proxy": 8099,
    "dev": 8097,
    "matrix": 8400
  }
}
