{
  "name": "tube-service",
  "version": "0.4.0",
  "description": "Injection and proxy for the tube app.",
  "main": "index.js",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "node build/build.js",
    "check:syntax": "node build/check-syntax.js dist/index.js",
    "test": "node test/injection.js && node test/routing.js && node test/proxying.js && node test/x509.js && node test/loader.js && node test/update-flow.js && node test/update-scheduling.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "node-fetch": "^2.7.0",
    "regenerator-runtime": "^0.14.1"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@vercel/ncc": "^0.38.3",
    "acorn": "^8.14.0"
  }
}
