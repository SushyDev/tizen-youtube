{
  "name": "tube-service",
  "version": "0.4.0",
  "description": "Injection and proxy for the tube app.",
  "main": "index.js",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "node ../tools/build-service.js",
    "test": "node test/injection.js && node test/upgrade.js && node test/chii.js && node test/cobalt.js && node test/routing.js && node test/rewrites.js && node test/proxying.js && node test/x509.js && node test/forward.js && node test/bigheaders.js && node test/loader.js && node test/update-flow.js && node test/update-scheduling.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "vite": "^8.2.2"
  }
}