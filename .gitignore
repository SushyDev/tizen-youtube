node_modules/
dist/
# `npm run dev` keeps the service's update cache here.
.dev/
release/
.ncc/
.package/
*.wgt
*.p12
*.log

# npm workspaces: only the root lockfile is authoritative.
*/package-lock.json
!/package-lock.json

# macOS finder metadata
.DS_Store
